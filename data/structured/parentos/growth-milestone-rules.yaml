table_family: product_catalog
owner: parentos
authority_class: product_authority_table
catalog_id: parentos.growth_milestone_rules
row_schema: rules[]
allowed_references: [.nimi/spec/parentos/canonical/*.authority.yaml, data/structured/parentos/*.yaml, data/knowledge/assets/*/asset.json]
forbidden_fields: [done, covered, coverage_status, audit_date, evidence_report, current, proposed, backlog_status, migration_status, mapping_status, run_id, ledger_ref]
version: 1
# Admitted by `.nimi/spec/parentos/canonical/growth-detail.authority.yaml#rule.parentos.growth.detail.r002`.
# Wave-0 of topic `2026-05-18-parentos-growth-curve-page-redesign`.
#
# This table is the only authority for growth milestone-event derivation
# semantics. The renderer reads only the generated TypeScript constants
# emitted from this file by `scripts/generate-knowledge-base.ts` and must
# not inline its own thresholds.
#
# This table defines NO metric ids. Every `appliesToMetricIds[j]` is a
# foreign key into `data/structured/parentos/health-metric-registry.yaml#metrics[].metricId`.
# The runner constraint `appliesToMetricIdsMustResolveInHealthMetricRegistry`
# is enforced by `scripts/check-parentos-knowledge-base.ts`.
constraints:
  ruleIdPattern: "^growth-milestone-[a-z0-9-]+$"
  uniqueRuleId: true
  appliesToMetricIdsMustResolveInHealthMetricRegistry: true
  evidenceWindowMonths:
    min: 1
    max: 24
rules: &parentos_growth_milestone_rule_entries
  - ruleId: growth-milestone-height-threshold-100cm
    kind: threshold_crossed
    appliesToMetricIds: [growth.height]
    triggerCondition:
      type: threshold_cross
      thresholdValue: 100
      thresholdUnit: cm
      direction: upward
      evidenceWindowMonths: 12
    titleTemplate: "突破 {thresholdValue} {thresholdUnit}"
    deltaMagnitudeTemplate: "+{deltaSinceLastYearRounded}"
    deltaUnitLabel: "CM · 12 月"
  - ruleId: growth-milestone-height-threshold-110cm
    kind: threshold_crossed
    appliesToMetricIds: [growth.height]
    triggerCondition:
      type: threshold_cross
      thresholdValue: 110
      thresholdUnit: cm
      direction: upward
      evidenceWindowMonths: 12
    titleTemplate: "突破 {thresholdValue} {thresholdUnit}"
    deltaMagnitudeTemplate: "+{deltaSinceLastYearRounded}"
    deltaUnitLabel: "CM · 12 月"
  - ruleId: growth-milestone-height-threshold-120cm
    kind: threshold_crossed
    appliesToMetricIds: [growth.height]
    triggerCondition:
      type: threshold_cross
      thresholdValue: 120
      thresholdUnit: cm
      direction: upward
      evidenceWindowMonths: 12
    titleTemplate: "突破 {thresholdValue} {thresholdUnit}"
    deltaMagnitudeTemplate: "+{deltaSinceLastYearRounded}"
    deltaUnitLabel: "CM · 12 月"
  - ruleId: growth-milestone-height-threshold-130cm
    kind: threshold_crossed
    appliesToMetricIds: [growth.height]
    triggerCondition:
      type: threshold_cross
      thresholdValue: 130
      thresholdUnit: cm
      direction: upward
      evidenceWindowMonths: 12
    titleTemplate: "突破 {thresholdValue} {thresholdUnit}"
    deltaMagnitudeTemplate: "+{deltaSinceLastYearRounded}"
    deltaUnitLabel: "CM · 12 月"
  - ruleId: growth-milestone-height-threshold-140cm
    kind: threshold_crossed
    appliesToMetricIds: [growth.height]
    triggerCondition:
      type: threshold_cross
      thresholdValue: 140
      thresholdUnit: cm
      direction: upward
      evidenceWindowMonths: 12
    titleTemplate: "突破 {thresholdValue} {thresholdUnit}"
    deltaMagnitudeTemplate: "+{deltaSinceLastYearRounded}"
    deltaUnitLabel: "CM · 12 月"
  - ruleId: growth-milestone-height-threshold-150cm
    kind: threshold_crossed
    appliesToMetricIds: [growth.height]
    triggerCondition:
      type: threshold_cross
      thresholdValue: 150
      thresholdUnit: cm
      direction: upward
      evidenceWindowMonths: 12
    titleTemplate: "突破 {thresholdValue} {thresholdUnit}"
    deltaMagnitudeTemplate: "+{deltaSinceLastYearRounded}"
    deltaUnitLabel: "CM · 12 月"
  - ruleId: growth-milestone-height-threshold-160cm
    kind: threshold_crossed
    appliesToMetricIds: [growth.height]
    triggerCondition:
      type: threshold_cross
      thresholdValue: 160
      thresholdUnit: cm
      direction: upward
      evidenceWindowMonths: 12
    titleTemplate: "突破 {thresholdValue} {thresholdUnit}"
    deltaMagnitudeTemplate: "+{deltaSinceLastYearRounded}"
    deltaUnitLabel: "CM · 12 月"
  - ruleId: growth-milestone-height-threshold-170cm
    kind: threshold_crossed
    appliesToMetricIds: [growth.height]
    triggerCondition:
      type: threshold_cross
      thresholdValue: 170
      thresholdUnit: cm
      direction: upward
      evidenceWindowMonths: 12
    titleTemplate: "突破 {thresholdValue} {thresholdUnit}"
    deltaMagnitudeTemplate: "+{deltaSinceLastYearRounded}"
    deltaUnitLabel: "CM · 12 月"
  # Weight rapid-change nodes. Unlike height (monotonic, absolute thresholds),
  # weight can swing either way, so these are relative_change rules: a >=10%
  # change versus the immediately preceding recorded weight. The upward rule
  # is a positive node; the downward rule is a negative node the parent
  # surface marks distinctly. These describe recorded data only (Layer 1) —
  # they carry no diagnostic or evaluative wording.
  - ruleId: growth-milestone-weight-rise-10pct
    kind: rapid_change
    appliesToMetricIds: [growth.weight]
    triggerCondition:
      type: relative_change
      changePercent: 10
      direction: upward
      valueUnit: kg
      evidenceWindowMonths: 24
    titleTemplate: "体重上升 {changeMagnitude}%"
    deltaMagnitudeTemplate: "+{deltaValueRounded}"
    deltaUnitLabel: "KG · 较上次"
  - ruleId: growth-milestone-weight-drop-10pct
    kind: rapid_change
    appliesToMetricIds: [growth.weight]
    triggerCondition:
      type: relative_change
      changePercent: 10
      direction: downward
      valueUnit: kg
      evidenceWindowMonths: 24
    titleTemplate: "体重下降 {changeMagnitude}%"
    deltaMagnitudeTemplate: "-{deltaValueRounded}"
    deltaUnitLabel: "KG · 较上次"

entries: *parentos_growth_milestone_rule_entries
